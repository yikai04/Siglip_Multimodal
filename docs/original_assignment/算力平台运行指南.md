# 算力平台运行指南


## 申请
+ 登录https://easycompute.cs.tsinghua.edu.cn，通过清华大学统一身份认证后，点击“登录领券/使用”按钮，就会自动发放1000元算力券，可在平台“我的优惠券”中查看及使用。
+ 根据计算需要，创建云服务器。
+ 如果算力已经用完或者有其他问题，可以找助教帮忙解决。


## 上传内容

将 `HW_SigLIP_2026` 目录上传到算力平台

## 环境安装
推荐使用uv进行环境管理

```bash
# 使用官方安装脚本
curl -LsSf https://astral.sh/uv/install.sh | sh
source ~/.bashrc
# 验证安装
uv --version

# 在项目目录中创建虚拟环境
cd /path/to/your/project

# 创建虚拟环境（默认在 .venv 目录）
uv venv

# 确保虚拟环境已激活
source .venv/bin/activate

# 或直接使用 uv pip（推荐，更快）
uv pip install -r requirements.txt
```